{
  "name": "pi-docker-sandbox",
  "version": "1.0.0",
  "description": "Lightweight Docker sandbox for pi coding agent",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "keywords": ["pi-package", "pi-extension", "sandbox", "docker"],
  "author": "Jack Senechal",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}